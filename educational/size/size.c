#include<stdio.h>
#include<stdbool.h>
main()
{
  int a;
  float b;
  char c;
  double d;
  printf("size of int is %ld",sizeof(a));
 printf("size of float is %ld",sizeof(b));
 printf("size of char is %ld",sizeof(c));
 printf("size of double is %ld",sizeof(d                                                                                                                                                                                                                                                  ));



}